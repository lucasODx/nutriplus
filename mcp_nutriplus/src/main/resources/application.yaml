spring:
  application:
    name: mcp_nutriplus
  ai:
    mcp:
      server:
        name: mcp_nutriplus
        protocol: STREAMABLE

servico:
  controle:
    base-url: ${SERVICO_CONTROLE_URL:http://localhost:8081}
  tabela:
    base-url: ${SERVICO_TABELA_URL:http://localhost:8082}
  pratica:
    base-url: ${SERVICO_PRATICA_URL:http://localhost:8083}

server:
  port: 8080